# Claude Notes

Follow `AGENTS.md`. This repository is public and distributed on npm as
`memlight`.

Useful commands:

```bash
npm ci
npm run type-check
npm test
npm run build
npm pack --dry-run
```

Before suggesting a release, verify that `package.json`, `README.md`,
`CHANGELOG.md`, and `RELEASING.md` agree on the version, package name, Node.js
runtime, and publish command.

